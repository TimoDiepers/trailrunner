# Data

Use `raw/` for original database files and supporting documents and `processed/` for derived files. The BAFU 2026 raw files and verified `processed/ecospold2-biosphere310/` export are tracked in Git. Other derived data payloads are ignored by default.
