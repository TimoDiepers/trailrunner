---
icon: lucide/map-pin
tags:
  - api
---

# Location Hierarchy

The child-to-parent map used to widen a parameter lookup: `CH -> RER -> GLO`. The chain always ends at the root, so every lookup has a last resort.

::: trailrunner.params.location
