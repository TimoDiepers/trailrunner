---
icon: lucide/package-check
tags:
  - api
---

# Result

What a model run returned: the `production` that must cover the triggering demand, the `technosphere` demands pushed onto the traversal queue, the `biosphere` exchanges accumulated into the inventory, and the `provenance` of the parameters used.

::: trailrunner.core.result
